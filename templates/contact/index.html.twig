{% extends 'base.html.twig' %}

{% block title %}Formulaire contact{% endblock %}

{% block body %}
    {{form_start(form)}}
        {{form_rest(form)}}
    {{form_end(form)}}
    <p>{{message}}</p>
{% endblock %}
