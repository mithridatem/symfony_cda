{% extends 'base.html.twig' %}

{% block title %}Mettre à jour Article{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_end(form) }}
    {% if msg is defined %}
        <p>{{ msg }}</p>
    {% endif %}
{% endblock %}