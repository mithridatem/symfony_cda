{% extends 'base.html.twig' %}

{% block title %}Ajouter Article{% endblock %}

{% block body %}
    <div>
        {{ form_start(form) }}
            {{ form_rest(form) }} 
        {{ form_end(form) }}
    </div>
    {% if msg is defined %}
        <p>{{ msg }}</p>
    {% endif %}
{% endblock %}